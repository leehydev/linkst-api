# docker-compose.yml
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: linkst
      POSTGRES_USER: linkst
      POSTGRES_PASSWORD: linkst
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: